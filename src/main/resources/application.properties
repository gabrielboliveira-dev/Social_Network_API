server.port=8080

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${DB_USERNAME:gabrielboliveira}
spring.datasource.password=${DB_PASSWORD:SherlockHolmes.221}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

file.upload-dir=./uploads

springdoc.swagger-ui.path=/swagger-ui.html

jwt.secret=${JWT_SECRET:YOUR_SECRET_KEY_HERE_FOR_SECURITY_IT_SHOULD_BE_VERY_LONG_AND_RANDOM}
jwt.expiration=${JWT_EXPIRATION:86400000}